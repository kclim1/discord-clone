const mongoose = require('mongoose')
const User = require('../models/userSchema.cjs')

const updateProfile = async ()=>{
    try{
         
    }catch(error){
        console.error('error uploading secure url',error)
    }
}

module.exports = updateProfile